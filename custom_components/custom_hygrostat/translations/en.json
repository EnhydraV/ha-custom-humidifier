{
  "title": "Custom Hygrostat",
  "config": {
    "step": {
      "user": {
        "title": "Custom Hygrostat",
        "description": "Configure your humidity-driven dehumidifier.\n\nCurrent template rendering:\n{preview}",
        "data": {
          "name": "Name",
          "target_sensor": "Humidity sensor",
          "dry_tolerance": "Dry tolerance",
          "wet_tolerance": "Wet tolerance",
          "min_humidity": "Min humidity (adjustable)",
          "max_humidity": "Max humidity (adjustable)",
          "min_cycle_duration": "Min cycle duration (min)",
          "startup_delay": "Maximum wait for inputs at HA startup (s, 0 = regulate immediately)",
          "enable_template": "Enable condition (template, empty = always enabled)",
          "error_template": "Error condition (template, true = stop, empty = ignored)",
          "boost_timer": "Boost timer (timer entity, active = boost)",
          "boost_humidity": "Boost target humidity",
          "device_entity": "Dehumidifier entity (driven by the hygrostat, and source of the internal sensor)",
          "power_switch": "Power switch (optional: power-cycles the device when it stops responding)",
          "fan_entity": "Device fan (optional, to drive the speed)",
          "fan_speed_template": "Fan speed (template returning a %, default 50)",
          "target_template": "Setpoint (template, e.g. {{ 55 }} or {{ states('sensor.setpoint')|float }})"
        }
      }
    },
    "error": {
      "humidity_range": "Minimum humidity must be lower than maximum.",
      "invalid_template": "Invalid template.",
      "self_reference": "This is the hygrostat's own entity: point to the appliance, not to its regulator.",
      "fan_entity_required": "A fan speed template requires selecting the fan entity.",
      "target_required": "The setpoint is required: a template, for instance {{ 55 }}."
    },
    "abort": {
      "already_configured": "A hygrostat with this name already exists."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Edit hygrostat",
        "data": {
          "name": "Name",
          "target_sensor": "Humidity sensor",
          "dry_tolerance": "Dry tolerance",
          "wet_tolerance": "Wet tolerance",
          "min_humidity": "Min humidity (adjustable)",
          "max_humidity": "Max humidity (adjustable)",
          "min_cycle_duration": "Min cycle duration (min)",
          "startup_delay": "Maximum wait for inputs at HA startup (s, 0 = regulate immediately)",
          "enable_template": "Enable condition (template, empty = always enabled)",
          "error_template": "Error condition (template, true = stop, empty = ignored)",
          "boost_timer": "Boost timer (timer entity, active = boost)",
          "boost_humidity": "Boost target humidity",
          "device_entity": "Dehumidifier entity (driven by the hygrostat, and source of the internal sensor)",
          "power_switch": "Power switch (optional: power-cycles the device when it stops responding)",
          "fan_entity": "Device fan (optional, to drive the speed)",
          "fan_speed_template": "Fan speed (template returning a %, default 50)",
          "target_template": "Setpoint (template, e.g. {{ 55 }} or {{ states('sensor.setpoint')|float }})"
        },
        "description": "Current template rendering:\n{preview}"
      }
    },
    "error": {
      "humidity_range": "Minimum humidity must be lower than maximum.",
      "invalid_template": "Invalid template.",
      "self_reference": "This is the hygrostat's own entity: point to the appliance, not to its regulator.",
      "fan_entity_required": "A fan speed template requires selecting the fan entity.",
      "target_required": "The setpoint is required: a template, for instance {{ 55 }}."
    }
  }
}
